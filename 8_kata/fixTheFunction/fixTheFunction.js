function addFive(num) {
    var total = num + 5;
    return total; //change num to total. Returning num will only give you the original number
}

///////// ------------------------ OR ------------------\\\\\\\\\\\\\\\\\\\\\\\

function addFive(num) {
  return num += 5
}